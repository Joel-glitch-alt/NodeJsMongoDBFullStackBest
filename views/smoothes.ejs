<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Smoothes Boutique</title>
    <style>
        /* General Styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Arial', sans-serif;
        }

        body {
            line-height: 1.6;
            background-color: #f9f9f9;
            color: #333;
        }

        a {
            text-decoration: none;
            color: inherit;
        }

        ul {
            list-style: none;
        }

        /* Navigation */
        nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem 2rem;
            background-color: #5d3a9b;
            color: #fff;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        nav h1 {
            font-size: 1.8rem;
            font-weight: bold;
        }

        nav ul {
            display: flex;
            gap: 1.5rem;
        }

        nav ul li {
            font-size: 1rem;
            font-weight: 500;
        }

        nav ul li a {
            color: #fff;
            transition: color 0.3s ease;
        }

        nav ul li a:hover {
            color: #ffd700;
        }

        .nav-buttons {
            display: flex;
            gap: 1rem;
        }

        .nav-buttons a {
            padding: 0.5rem 1rem;
            background-color: #ffd700;
            color: #333;
            border-radius: 4px;
            font-size: 0.9rem;
            font-weight: bold;
            transition: background-color 0.3s ease;
        }

        .nav-buttons a:hover {
            background-color: #e5c100;
        }

        /* Hero Section */
        .hero {
            text-align: center;
            padding: 4rem 2rem;
            background: linear-gradient(135deg, #5d3a9b, #8e44ad);
            color: #fff;
        }

        .hero h2 {
            font-size: 3rem;
            margin-bottom: 1rem;
        }

        .hero p {
            font-size: 1.2rem;
            margin-bottom: 2rem;
        }

        .hero button {
            padding: 0.8rem 2rem;
            background-color: #ffd700;
            color: #333;
            font-weight: bold;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .hero button:hover {
            background-color: #e5c100;
        }

        /* Footer */
        footer {
            text-align: center;
            padding: 1rem;
            background-color: #5d3a9b;
            color: #fff;
            margin-top: 2rem;
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav>
        <h1><a href="/">Addition's Boutique</a></h1>
        <ul>
            <li><a href="#home">Home</a></li>
            <li><a href="#products">Products</a></li>
            <li><a href="#about">About</a></li>
            <li><a href="#contact">Contact</a></li>
        </ul>
        <div class="nav-buttons">
            <% if (user) { %>
                <span>Welcome, <%= user.Username %></span>
                <a href="/logout">Log Out</a>
            <% } else { %>
                <a href="/login">Login</a>
                <a href="/signup">Sign Up</a>
            <% } %>
        </div>
    </nav>
    

    <!-- Hero Section -->
    <section class="hero" id="home">
        <h2>Welcome to ADDITION Boutique</h2>
        <p>Discover your style with our exclusive collection.</p>
        <button>Shop Now</button>
    </section>

    <!-- Footer -->
    <footer>
        <p>&copy; 2024 Addition Boutique. All Rights Reserved.</p>
    </footer>
</body>
</html>
